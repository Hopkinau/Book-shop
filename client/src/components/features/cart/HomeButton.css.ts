import { style } from '@vanilla-extract/css';
import { vars } from '../../../styles/themes.css';
export const title = style({
  display: 'flex',
  position: 'absolute',
  top: '250px',
  right: '20px',
  zIndex: 1,
});
